Drop your trained model here as `yolo_model.tflite`.

Until then the app runs in Demo mode (simulated detections).

labels.txt in this folder lists the class order used during training —
line 0 = class 0, line 1 = class 1, etc. These strings must match the
"id" fields in ../data/exercises.json.
